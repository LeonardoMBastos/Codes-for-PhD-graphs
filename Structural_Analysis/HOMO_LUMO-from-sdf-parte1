sudo apt update
sudo apt install -y python3-pip python3-venv build-essential libgomp1

mkdir -p ~/homo_lumo
cd ~/homo_lumo

python3 -m venv .venv
source .venv/bin/activate

python -m pip install --upgrade pip
pip install numpy pandas rdkit pyscf
